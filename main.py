import streamlit as st

# استایل برای راست‌چین کردن متن‌ها
st.markdown(""" <style> .main {direction: rtl; text-align: right;} </style> """, unsafe_allow_html=True)

st.title("🛡️ سامانه سناریوساز هوشمند عاشورا")
st.write("خوش آمدید! لطفاً اطلاعات زیر را تکمیل کنید تا سناریو آماده شود.")

# ورودی‌های کاربر
category = st.selectbox("انتخاب حوزه:", ["فنی", "مالی", "HSSE", "ماشین‌آلات"])
issue = st.text_area("شرح چالش یا مسئله فنی:")

if st.button("تولید سناریو"):
    if issue:
        st.success(f"سناریوی پیشنهادی برای واحد {category} بر اساس نشریات مصوب آماده شد.")
        st.markdown(f"**خلاصه سناریو:** با توجه به چالش `{issue}`، این ویدیو در ۳ بخش تولید شود.")
    else:
        st.error("لطفاً ابتدا چالش فنی را بنویسید.")
